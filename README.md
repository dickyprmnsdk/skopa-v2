# Skopa v2

Marketplace freelance mahasiswa — rebuild dengan stack Laravel API + React + PostgreSQL + Docker.

## Struktur

- `backend/` — Laravel REST API
- `frontend/` — React + Vite
- `docs/` — Dokumentasi (lihat PRD.md)

## Menjalankan

docker compose up -d
